{
  "name": "berisa-platform",
  "private": true,
  "version": "3.0.0",
  "description": "Berisa commercial intelligence platform",
  "scripts": {
    "dev:api": "cd backend && npm run dev",
    "dev:web": "cd frontend && npm run dev",
    "seed": "cd backend && npm run seed",
    "migrate": "cd backend && npm run migrate",
    "create-env": "cd backend && npm run create-env"
  }
}